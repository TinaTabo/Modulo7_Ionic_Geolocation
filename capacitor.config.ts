import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'io.ionic.starter',
  appName: 'Modulo7_Ionic_Geolocation',
  webDir: 'www',
  server: {
    androidScheme: 'https'
  }
};

export default config;
